export const metadata = {
  title: "My Bookings | Care Services",
  description: "Baby Sitting & Elderly Care Service Platform",
};

export default function BookingsLayout({ children }) {
  return children;
}